# Декомпозиция

[![Vercel Deploy](https://img.shields.io/badge/vercel-deployed-success?style=flat&logo=vercel)](https://ra-homeworks-5-composition-decomposition-j0n7b8xzl.vercel.app)
[![Live Demo](https://img.shields.io/badge/demo-live-success?style=flat&logo=vercel)](https://ra-homeworks-5-composition-decomposition-j0n7b8xzl.vercel.app)

Учебный проект, демонстрирующий принципы декомпозиции сложного интерфейса на переиспользуемые React-компоненты. Включает реализацию новостного агрегатора с различными виджетами.

## ✨ Особенности

- Компонентный подход с декомпозицией на переиспользуемые части
- Адаптивный дизайн для всех устройств
- Различные типы контента (новости, погода, курс валют, телепрограмма, эфир)
- Валидация пропсов через PropTypes
- Поддержка доступности (a11y) для пользователей скринридеров
- Модульная структура проекта

## 🛠 Технологии

- React 19+
- Vite
- CSS3 (модули)
- PropTypes
- ESLint

## ⚙️ Установка и запуск

1. Установите зависимости:

```bash
npm install
```

2. Запустите приложение в режиме разработки:

```bash
npm run dev
```

3. Соберите для продакшена:

```bash
npm run build
```

4. Для предпросмотра продакшн-сборки:

```bash
npm run preview
```

## 📁 Структура проекта

```
src/
├── assets/               # Статические файлы
├── components/           # React-компоненты
│   ├── layout/           # Компоненты макета
│   ├── news/             # Компоненты новостей
│   ├── shared/           # Общие компоненты
│   └── widgets/          # Виджеты (погода, курс валют и т.д.)
├── data/                 # Моковые данные
├── styles/               # Стили
│   └── main.css          # Основные стили
├── App.jsx               # Корневой компонент
└── main.jsx              # Точка входа
```

## 🔧 Использование

### Основные компоненты

```jsx
// Импорт компонентов
import {
  NewsList,
  WeatherWidget,
  CurrencyRate,
  NavigationTabs,
  SearchBar
} from './components';

// Использование в приложении
<>
  <NewsList items={news} />
  <WeatherWidget data={weather} />
  <CurrencyRate rates={currency} />
  <NavigationTabs tabs={navigation} />
  <SearchBar placeholder="Поиск..." />
</>
```

## 📝 Лицензия

Учебный проект в рамках курса [Нетологии](https://netology.ru/).
